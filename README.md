# Simple phonebook manager
### Usage:
#### Using command-line arguments

> Note: You need to have a `contacts`.`csv` file in the same directory as `phonebook`

```sh
phonebook search
phonebook add
```
#### requirements:

+ All you need is python3 or python

